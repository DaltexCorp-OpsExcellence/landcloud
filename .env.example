# Copy this file to .env (which is gitignored) and fill in the real password.
# Get the string from: Supabase Dashboard → Project Settings → Database →
# Connection string → URI  (use the "Session pooler", port 5432).
# The password stays in .env on your machine only — never commit it, never paste it in chat.

DATABASE_URL=postgresql://postgres.sfyjvgjwvtwkrnqrvqyc:YOUR_DB_PASSWORD@aws-1-eu-west-1.pooler.supabase.com:5432/postgres
